-- Generated by scripts/collections.mjs. Edit the shared template there.

mu TxEnvelopes : Type 0 :=
| txEnvelopesNil : TxEnvelopes
| txEnvelopesCons (head : TxEnvelope) (tail : TxEnvelopes) : TxEnvelopes

def seqTxEnvelopeEmpty : TxEnvelopes := txEnvelopesNil
def seqTxEnvelopeCons : TxEnvelope -> TxEnvelopes -> TxEnvelopes := fun (head : TxEnvelope) (tail : TxEnvelopes) => txEnvelopesCons head tail
def seqTxEnvelopeHead : TxEnvelopes -> ValueOption TxEnvelope := fun (xs : TxEnvelopes) =>
  match xs as self in TxEnvelopes return ValueOption TxEnvelope with
  | txEnvelopesNil => valueNone (TxEnvelope)
  | txEnvelopesCons head tail => valueSome (TxEnvelope) head
def seqTxEnvelopeTail : TxEnvelopes -> TxEnvelopes := fun (xs : TxEnvelopes) =>
  match xs as self in TxEnvelopes return TxEnvelopes with
  | txEnvelopesNil => txEnvelopesNil
  | txEnvelopesCons head tail => tail
def rec seqTxEnvelopeRevAcc : TxEnvelopes -> TxEnvelopes -> TxEnvelopes := fun (xs : TxEnvelopes) (acc : TxEnvelopes) =>
  match xs as self in TxEnvelopes return TxEnvelopes with
  | txEnvelopesNil => acc
  | txEnvelopesCons head tail => seqTxEnvelopeRevAcc tail (txEnvelopesCons head acc)
def seqTxEnvelopeReverse : TxEnvelopes -> TxEnvelopes := fun (xs : TxEnvelopes) => seqTxEnvelopeRevAcc xs txEnvelopesNil
def rec seqTxEnvelopeLengthAcc : TxEnvelopes -> Nat -> Nat := fun (xs : TxEnvelopes) (acc : Nat) =>
  match xs as self in TxEnvelopes return Nat with
  | txEnvelopesNil => acc
  | txEnvelopesCons head tail => seqTxEnvelopeLengthAcc tail (natAdd acc 1)
def seqTxEnvelopeLength : TxEnvelopes -> Nat := fun (xs : TxEnvelopes) => seqTxEnvelopeLengthAcc xs 0
def rec seqTxEnvelopeWriteAcc : TxEnvelopes -> (TxEnvelope -> Bytes) -> Bytes -> Bytes :=
  fun (xs : TxEnvelopes) (write : TxEnvelope -> Bytes) (acc : Bytes) =>
    match xs as self in TxEnvelopes return Bytes with
    | txEnvelopesNil => reverse acc
    | txEnvelopesCons head tail => seqTxEnvelopeWriteAcc tail write (reverseOnto (write head) acc)
def seqTxEnvelopeWrite : (TxEnvelope -> Bytes) -> TxEnvelopes -> Bytes :=
  fun (write : TxEnvelope -> Bytes) (xs : TxEnvelopes) => seqTxEnvelopeWriteAcc xs write bytesNil
def rec seqTxEnvelopeInsert : TxEnvelopes -> (TxEnvelope -> TxEnvelope -> Ordering) -> TxEnvelope -> TxEnvelopes :=
  fun (xs : TxEnvelopes) (compare : TxEnvelope -> TxEnvelope -> Ordering) (value : TxEnvelope) =>
    match xs as self in TxEnvelopes return TxEnvelopes with
    | txEnvelopesNil => txEnvelopesCons value txEnvelopesNil
    | txEnvelopesCons head tail =>
      match (compare value head) as compared in Ordering return TxEnvelopes with
      | orderLess => txEnvelopesCons value xs
      | orderEqual => txEnvelopesCons value xs
      | orderGreater => txEnvelopesCons head (seqTxEnvelopeInsert tail compare value)
def rec seqTxEnvelopeSortScan : TxEnvelopes -> (TxEnvelope -> TxEnvelope -> Ordering) -> TxEnvelopes :=
  fun (xs : TxEnvelopes) (compare : TxEnvelope -> TxEnvelope -> Ordering) =>
    match xs as self in TxEnvelopes return TxEnvelopes with
    | txEnvelopesNil => txEnvelopesNil
    | txEnvelopesCons head tail => seqTxEnvelopeInsert (seqTxEnvelopeSortScan tail compare) compare head
def seqTxEnvelopeSort : (TxEnvelope -> TxEnvelope -> Ordering) -> TxEnvelopes -> TxEnvelopes :=
  fun (compare : TxEnvelope -> TxEnvelope -> Ordering) (xs : TxEnvelopes) => seqTxEnvelopeSortScan xs compare
def rec seqTxEnvelopeUniqScan : TxEnvelopes -> (TxEnvelope -> TxEnvelope -> Ordering) -> ValueOption (TxEnvelope) -> TxEnvelopes -> TxEnvelopes :=
  fun (xs : TxEnvelopes) (compare : TxEnvelope -> TxEnvelope -> Ordering) (previous : ValueOption (TxEnvelope)) (acc : TxEnvelopes) =>
    match xs as self in TxEnvelopes return TxEnvelopes with
    | txEnvelopesNil => seqTxEnvelopeReverse acc
    | txEnvelopesCons head tail =>
      let duplicate : Nat :=
        case previous with
        | 0 (unit : prod ()) => 0
        | 1 (value : TxEnvelope) =>
          match (compare value head) as compared in Ordering return Nat with
          | orderLess => 0
          | orderGreater => 0
          | orderEqual => 1
      in
      case (natEq duplicate 1) with
      | 0 (unit : prod ()) => seqTxEnvelopeUniqScan tail compare (valueSome (TxEnvelope) head) (txEnvelopesCons head acc)
      | 1 (unit : prod ()) => seqTxEnvelopeUniqScan tail compare previous acc
def seqTxEnvelopeUniq : (TxEnvelope -> TxEnvelope -> Ordering) -> TxEnvelopes -> TxEnvelopes :=
  fun (compare : TxEnvelope -> TxEnvelope -> Ordering) (xs : TxEnvelopes) => seqTxEnvelopeUniqScan xs compare (valueNone (TxEnvelope)) txEnvelopesNil
def seqTxEnvelopeOps : ListOps (TxEnvelope) TxEnvelopes :=
  tuple (seqTxEnvelopeEmpty, seqTxEnvelopeCons, seqTxEnvelopeReverse, seqTxEnvelopeLength, seqTxEnvelopeHead, seqTxEnvelopeTail, seqTxEnvelopeWrite, seqTxEnvelopeSort, seqTxEnvelopeUniq)

mu EvmTransactions : Type 0 :=
| evmTransactionsNil : EvmTransactions
| evmTransactionsCons (head : Transaction) (tail : EvmTransactions) : EvmTransactions

def seqEvmTransactionEmpty : EvmTransactions := evmTransactionsNil
def seqEvmTransactionCons : Transaction -> EvmTransactions -> EvmTransactions := fun (head : Transaction) (tail : EvmTransactions) => evmTransactionsCons head tail
def seqEvmTransactionHead : EvmTransactions -> ValueOption Transaction := fun (xs : EvmTransactions) =>
  match xs as self in EvmTransactions return ValueOption Transaction with
  | evmTransactionsNil => valueNone (Transaction)
  | evmTransactionsCons head tail => valueSome (Transaction) head
def seqEvmTransactionTail : EvmTransactions -> EvmTransactions := fun (xs : EvmTransactions) =>
  match xs as self in EvmTransactions return EvmTransactions with
  | evmTransactionsNil => evmTransactionsNil
  | evmTransactionsCons head tail => tail
def rec seqEvmTransactionRevAcc : EvmTransactions -> EvmTransactions -> EvmTransactions := fun (xs : EvmTransactions) (acc : EvmTransactions) =>
  match xs as self in EvmTransactions return EvmTransactions with
  | evmTransactionsNil => acc
  | evmTransactionsCons head tail => seqEvmTransactionRevAcc tail (evmTransactionsCons head acc)
def seqEvmTransactionReverse : EvmTransactions -> EvmTransactions := fun (xs : EvmTransactions) => seqEvmTransactionRevAcc xs evmTransactionsNil
def rec seqEvmTransactionLengthAcc : EvmTransactions -> Nat -> Nat := fun (xs : EvmTransactions) (acc : Nat) =>
  match xs as self in EvmTransactions return Nat with
  | evmTransactionsNil => acc
  | evmTransactionsCons head tail => seqEvmTransactionLengthAcc tail (natAdd acc 1)
def seqEvmTransactionLength : EvmTransactions -> Nat := fun (xs : EvmTransactions) => seqEvmTransactionLengthAcc xs 0
def rec seqEvmTransactionWriteAcc : EvmTransactions -> (Transaction -> Bytes) -> Bytes -> Bytes :=
  fun (xs : EvmTransactions) (write : Transaction -> Bytes) (acc : Bytes) =>
    match xs as self in EvmTransactions return Bytes with
    | evmTransactionsNil => reverse acc
    | evmTransactionsCons head tail => seqEvmTransactionWriteAcc tail write (reverseOnto (write head) acc)
def seqEvmTransactionWrite : (Transaction -> Bytes) -> EvmTransactions -> Bytes :=
  fun (write : Transaction -> Bytes) (xs : EvmTransactions) => seqEvmTransactionWriteAcc xs write bytesNil
def rec seqEvmTransactionInsert : EvmTransactions -> (Transaction -> Transaction -> Ordering) -> Transaction -> EvmTransactions :=
  fun (xs : EvmTransactions) (compare : Transaction -> Transaction -> Ordering) (value : Transaction) =>
    match xs as self in EvmTransactions return EvmTransactions with
    | evmTransactionsNil => evmTransactionsCons value evmTransactionsNil
    | evmTransactionsCons head tail =>
      match (compare value head) as compared in Ordering return EvmTransactions with
      | orderLess => evmTransactionsCons value xs
      | orderEqual => evmTransactionsCons value xs
      | orderGreater => evmTransactionsCons head (seqEvmTransactionInsert tail compare value)
def rec seqEvmTransactionSortScan : EvmTransactions -> (Transaction -> Transaction -> Ordering) -> EvmTransactions :=
  fun (xs : EvmTransactions) (compare : Transaction -> Transaction -> Ordering) =>
    match xs as self in EvmTransactions return EvmTransactions with
    | evmTransactionsNil => evmTransactionsNil
    | evmTransactionsCons head tail => seqEvmTransactionInsert (seqEvmTransactionSortScan tail compare) compare head
def seqEvmTransactionSort : (Transaction -> Transaction -> Ordering) -> EvmTransactions -> EvmTransactions :=
  fun (compare : Transaction -> Transaction -> Ordering) (xs : EvmTransactions) => seqEvmTransactionSortScan xs compare
def rec seqEvmTransactionUniqScan : EvmTransactions -> (Transaction -> Transaction -> Ordering) -> ValueOption (Transaction) -> EvmTransactions -> EvmTransactions :=
  fun (xs : EvmTransactions) (compare : Transaction -> Transaction -> Ordering) (previous : ValueOption (Transaction)) (acc : EvmTransactions) =>
    match xs as self in EvmTransactions return EvmTransactions with
    | evmTransactionsNil => seqEvmTransactionReverse acc
    | evmTransactionsCons head tail =>
      let duplicate : Nat :=
        case previous with
        | 0 (unit : prod ()) => 0
        | 1 (value : Transaction) =>
          match (compare value head) as compared in Ordering return Nat with
          | orderLess => 0
          | orderGreater => 0
          | orderEqual => 1
      in
      case (natEq duplicate 1) with
      | 0 (unit : prod ()) => seqEvmTransactionUniqScan tail compare (valueSome (Transaction) head) (evmTransactionsCons head acc)
      | 1 (unit : prod ()) => seqEvmTransactionUniqScan tail compare previous acc
def seqEvmTransactionUniq : (Transaction -> Transaction -> Ordering) -> EvmTransactions -> EvmTransactions :=
  fun (compare : Transaction -> Transaction -> Ordering) (xs : EvmTransactions) => seqEvmTransactionUniqScan xs compare (valueNone (Transaction)) evmTransactionsNil
def seqEvmTransactionOps : ListOps (Transaction) EvmTransactions :=
  tuple (seqEvmTransactionEmpty, seqEvmTransactionCons, seqEvmTransactionReverse, seqEvmTransactionLength, seqEvmTransactionHead, seqEvmTransactionTail, seqEvmTransactionWrite, seqEvmTransactionSort, seqEvmTransactionUniq)

mu BlockReceipts : Type 0 :=
| blockReceiptsNil : BlockReceipts
| blockReceiptsCons (head : Receipt) (tail : BlockReceipts) : BlockReceipts

def seqBlockReceiptEmpty : BlockReceipts := blockReceiptsNil
def seqBlockReceiptCons : Receipt -> BlockReceipts -> BlockReceipts := fun (head : Receipt) (tail : BlockReceipts) => blockReceiptsCons head tail
def seqBlockReceiptHead : BlockReceipts -> ValueOption Receipt := fun (xs : BlockReceipts) =>
  match xs as self in BlockReceipts return ValueOption Receipt with
  | blockReceiptsNil => valueNone (Receipt)
  | blockReceiptsCons head tail => valueSome (Receipt) head
def seqBlockReceiptTail : BlockReceipts -> BlockReceipts := fun (xs : BlockReceipts) =>
  match xs as self in BlockReceipts return BlockReceipts with
  | blockReceiptsNil => blockReceiptsNil
  | blockReceiptsCons head tail => tail
def rec seqBlockReceiptRevAcc : BlockReceipts -> BlockReceipts -> BlockReceipts := fun (xs : BlockReceipts) (acc : BlockReceipts) =>
  match xs as self in BlockReceipts return BlockReceipts with
  | blockReceiptsNil => acc
  | blockReceiptsCons head tail => seqBlockReceiptRevAcc tail (blockReceiptsCons head acc)
def seqBlockReceiptReverse : BlockReceipts -> BlockReceipts := fun (xs : BlockReceipts) => seqBlockReceiptRevAcc xs blockReceiptsNil
def rec seqBlockReceiptLengthAcc : BlockReceipts -> Nat -> Nat := fun (xs : BlockReceipts) (acc : Nat) =>
  match xs as self in BlockReceipts return Nat with
  | blockReceiptsNil => acc
  | blockReceiptsCons head tail => seqBlockReceiptLengthAcc tail (natAdd acc 1)
def seqBlockReceiptLength : BlockReceipts -> Nat := fun (xs : BlockReceipts) => seqBlockReceiptLengthAcc xs 0
def rec seqBlockReceiptWriteAcc : BlockReceipts -> (Receipt -> Bytes) -> Bytes -> Bytes :=
  fun (xs : BlockReceipts) (write : Receipt -> Bytes) (acc : Bytes) =>
    match xs as self in BlockReceipts return Bytes with
    | blockReceiptsNil => reverse acc
    | blockReceiptsCons head tail => seqBlockReceiptWriteAcc tail write (reverseOnto (write head) acc)
def seqBlockReceiptWrite : (Receipt -> Bytes) -> BlockReceipts -> Bytes :=
  fun (write : Receipt -> Bytes) (xs : BlockReceipts) => seqBlockReceiptWriteAcc xs write bytesNil
def rec seqBlockReceiptInsert : BlockReceipts -> (Receipt -> Receipt -> Ordering) -> Receipt -> BlockReceipts :=
  fun (xs : BlockReceipts) (compare : Receipt -> Receipt -> Ordering) (value : Receipt) =>
    match xs as self in BlockReceipts return BlockReceipts with
    | blockReceiptsNil => blockReceiptsCons value blockReceiptsNil
    | blockReceiptsCons head tail =>
      match (compare value head) as compared in Ordering return BlockReceipts with
      | orderLess => blockReceiptsCons value xs
      | orderEqual => blockReceiptsCons value xs
      | orderGreater => blockReceiptsCons head (seqBlockReceiptInsert tail compare value)
def rec seqBlockReceiptSortScan : BlockReceipts -> (Receipt -> Receipt -> Ordering) -> BlockReceipts :=
  fun (xs : BlockReceipts) (compare : Receipt -> Receipt -> Ordering) =>
    match xs as self in BlockReceipts return BlockReceipts with
    | blockReceiptsNil => blockReceiptsNil
    | blockReceiptsCons head tail => seqBlockReceiptInsert (seqBlockReceiptSortScan tail compare) compare head
def seqBlockReceiptSort : (Receipt -> Receipt -> Ordering) -> BlockReceipts -> BlockReceipts :=
  fun (compare : Receipt -> Receipt -> Ordering) (xs : BlockReceipts) => seqBlockReceiptSortScan xs compare
def rec seqBlockReceiptUniqScan : BlockReceipts -> (Receipt -> Receipt -> Ordering) -> ValueOption (Receipt) -> BlockReceipts -> BlockReceipts :=
  fun (xs : BlockReceipts) (compare : Receipt -> Receipt -> Ordering) (previous : ValueOption (Receipt)) (acc : BlockReceipts) =>
    match xs as self in BlockReceipts return BlockReceipts with
    | blockReceiptsNil => seqBlockReceiptReverse acc
    | blockReceiptsCons head tail =>
      let duplicate : Nat :=
        case previous with
        | 0 (unit : prod ()) => 0
        | 1 (value : Receipt) =>
          match (compare value head) as compared in Ordering return Nat with
          | orderLess => 0
          | orderGreater => 0
          | orderEqual => 1
      in
      case (natEq duplicate 1) with
      | 0 (unit : prod ()) => seqBlockReceiptUniqScan tail compare (valueSome (Receipt) head) (blockReceiptsCons head acc)
      | 1 (unit : prod ()) => seqBlockReceiptUniqScan tail compare previous acc
def seqBlockReceiptUniq : (Receipt -> Receipt -> Ordering) -> BlockReceipts -> BlockReceipts :=
  fun (compare : Receipt -> Receipt -> Ordering) (xs : BlockReceipts) => seqBlockReceiptUniqScan xs compare (valueNone (Receipt)) blockReceiptsNil
def seqBlockReceiptOps : ListOps (Receipt) BlockReceipts :=
  tuple (seqBlockReceiptEmpty, seqBlockReceiptCons, seqBlockReceiptReverse, seqBlockReceiptLength, seqBlockReceiptHead, seqBlockReceiptTail, seqBlockReceiptWrite, seqBlockReceiptSort, seqBlockReceiptUniq)
